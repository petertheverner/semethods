sudo: required

language: java

services:
  - docker

## Parameter that makes docker end all services when 1 container is finished
## (To close the database container once the application container has finished running).
after_success:
  - docker-compose up --abort-on-container-exit